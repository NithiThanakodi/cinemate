export const Button = ({ children }) => {
    return (
        <button className="w-64 rounded-lg bg-gradient-to-r from-blue-500 via-blue-600 to-blue-700 py-2.5 text-white font-medium mr-2 mb-2"> {children}</button>
    )
}
